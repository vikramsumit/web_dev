USE practical;

DROP TABLE Employee;

CREATE TABLE Employee(
	EMPLOYEE_ID Int,
    LAST_NAME VARCHAR(50),
    JOB_ID VARCHAR(20),
    SALARY Int,
    COMM_PCT DECIMAL(4,2),
    DEPARTMENT_ID INT
);

INSERT INTO Employee(EMPLOYEE_ID, LAST_NAME, JOB_ID, SALARY, COMM_PCT, DEPARTMENT_ID) VALUES
(198, 'Cornell', 'SH_CLERK', 2600, 2.5, 50),
(199, 'Grant', 'SH_CLERK', 2600, 2.2, 50),
(200, 'Whalen', 'AD_ASST', 4400, 1.3, 10),
(201, 'Hartstein', 'IT_PROG', 6000, NULL, 20),
(202, 'Fay', 'AC_MGR', 6500, NULL, 20),
(203, 'Mavris', 'AD_VP', 7500, NULL, 40),
(204, 'Baer', 'AD_PRES', 3500, 1.5, 90),
(205, 'Higgins', 'AC_MGR', 7200, NULL, 60),
(206, 'Gitz', 'IT_PROG', 5000, NULL, 60),
(100, 'King', 'AD_ASST', 8955, 0.3, 100),
(101, 'Kochhar', 'SH_CLERK', 3400, 1.3, 30);

SELECT * FROM Employee;

SELECT LAST_NAME, SALARY, SALARY + 300 as INC_SALARY
FROM Employee;

SELECT LAST_NAME, SALARY
FROM Employee
WHERE SALARY BETWEEN 4000 AND 7000;

SELECT JOB_ID
FROM Employee
WHERE SALARY > 5000;

UPDATE Employee
SET JOB_ID = 'Grade A'
WHERE SALARY > 5000;

SELECT * FROM Employee;

SELECT LAST_NAME, length(LAST_NAME) AS nlength
FROM Employee
WHERE COMM_PCT IS NOT NULL;

SELECT LAST_NAME, JOB_ID, SALARY
FROM Employee
WHERE SALARY BETWEEN 4000 AND 7000;

SELECT LOWER(LAST_NAME)
FROM Employee
WHERE DEPARTMENT_ID IN(10,20,30);